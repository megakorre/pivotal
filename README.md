# pivotal-tracker utils in emacs using api version 5

## settings
~/.pivotal-api-key = Your api key
~/.pivotal-project = Your pivotal project

* `pivotal-make-ref`
  Promts for active stories and
  inserts a [id] style ref into the current buffer
